#!/bin/sh

if [ ! -d /mnt/ic-http-lb ]; then
    sleep 86400
    exit
fi

LOG_DIR='/var/log/ic-http-lb'

# Create log dir
if [ ! -d $LOG_DIR ]; then
    mkdir -p $LOG_DIR
fi

exec chpst -uroot svlogd -tt $LOG_DIR
